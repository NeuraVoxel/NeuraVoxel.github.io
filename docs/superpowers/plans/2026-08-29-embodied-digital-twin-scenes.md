# 具身智能 · 数字孪生空场景 · 实现计划

**日期:** 2026-08-29  
**规格:** `docs/superpowers/specs/2026-08-29-embodied-digital-twin-scenes-design.md`

## 任务

1. 更新 `src/data/workshop.zh.json`：在 `driving` 与 `learning` 之间插入 `embodied`、`digital-twin` 两个 `empty` 场景（字段见规格）。
2. 更新 `src/pages/index.astro`：在自动驾驶卡之后插入两张 muted 场景卡（文案与链接见规格）。
3. 更新 `src/pages/workshop/index.astro`：在 Tab「自动驾驶」之后插入 `embodied`、`digital-twin` 两个筛选项。
4. 更新 `src/components/zeyo/ZeyoFooter.astro`：工坊列在「自动驾驶」之后增加两条链接。
5. 更新 `src/pages/contact/index.astro`：合作方向增加两条规划中条目；页头场景列举与之对齐。
6. 本地核对首页顺序、`/workshop/#embodied`、`/workshop/#digital-twin`、页脚与联系页。
